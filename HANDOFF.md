# 东京真实电车大冒险 — 当前状态交接

更新：2026-09-20。用户要求暂停功能开发，本次只验证、建 Git、记录和备份。没有重构、删除功能或修改游戏源码。

## 版本与恢复

- 当前游戏代码的基线 commit hash：`c23ec8b5439dedf79160bdbea7e4cb2eb6862a3d`。
- 补记 hash 的后续提交只修改本文档；最新仓库 HEAD 用 `git rev-parse HEAD` 查看。Git 提交不能在自身受跟踪内容中包含自身最终 hash。
- 本机仓库：`/Users/ri/Desktop/dianche`，分支 `main`；本次之前没有 Git。没有推送到远程。
- 未配置 Git 作者身份，备份使用临时提交身份 `Project Backup <backup@localhost>`，不修改全局 Git 配置。
- 整个现有项目、历史源码备份、截图、测试及本次检查日志均保存，不清理旧文件。Git 备份不包含浏览器内的个人 localStorage 存档。

## 启动与已完成的主要功能

双击 `index.html` 或 `开始游戏.command`。纯 HTML/CSS/JavaScript Canvas，可离线运行，不需要 npm 安装、构建或本地服务器。

- 家 → 京王堀之内 → 南大沢 → 三井アウトレットパーク扭蛋 → 京王堀之内 → 家的完整任务；步行、刷 IC、充值、候车、上下车、收藏和奖励。
- 京王新宿 ↔ 橋本 / 京王八王子 / 高尾山口直通；調布与北野按分岔运行，調布无需换车。各停、快速、区間急行、特急、ライナー有不同停车规则。
- 列车实际移动，车内自由走动与空座互动、乘客上下车和随机座位、高峰拥挤、手动下车、1×/2×/4×时间快进；ライナー售票机和检票。
- 相模原线沿线生活圈、多摩モノレール7站、京王動物園線、步行换乘、地方任务与站印章。
- 林俊芳孕期休息、大伟博做饭/工作/打扫、玩具、昼夜和17时钟声；公园鸭子、しゃぶ葉送餐机器人。
- 秋葉台公園金字塔与长滑梯；麦当劳5款、三和6款食品购物，带回家摆桌食用与煎肉，购物和待烹饪食品保存。
- 键盘、点击步行、触屏按钮、儿童提示、自由探索、日语字幕及浏览器语音/合成音效。

## 本次运行检查

记录在 `artifacts/handoff-validation/`，历史第10版验证记录在 `artifacts/v10-test-results.txt`。为了保留原截图，测试工作目录是临时完整副本；现有测试的固定 file URL 仍启动原项目，部分系统场景使用相同源码副本。没有改写原测试。

- PASS：`node --check game.js`、`node --check data.js`、`zsh -n 开始游戏.command`。
- PASS：实际执行 `zsh 开始游戏.command`，退出码0；自动化测试在 Chrome 中启动并操作游戏。
- PASS：`tests/v10-life.cjs --mobile` — 11种食品、扣币、摆桌、烹饪、吃饭、滑梯往返、重开保存。
- PASS：`tests/v10-systems.cjs` — 36个班次样本的分岔/方向/时序/停车，ライナー购票与上下车限制、高峰座位、手机4×、高幡不動京王闸内换乘。
- PASS：`tests/v9-migration.cjs` — 旧印章索引仅迁移一次，余额和家庭进度保留。
- PASS：`tests/v10-rail.cjs` — 扭蛋完整往返、IC扣款、奖励、快进，以及同车经过調布到新宿、八王子/高尾山口直通往返；日志两段PASS且无错误输出。
- 测试使用现有 Node v24.19.0、Playwright 与本机 Google Chrome；已通过用例均检查浏览器脚本错误。未将历史全部旧测试视为当前版本回归套件。

## 已知问题与未完成项

- 本次已通过检查未发现阻断运行的 bug；不是所有地点、设备、浏览器及音频均重新测试，不能据此声称无 bug。
- 时刻、运行时间、80円车费、100游戏币ライナー券、自由选座及站场布局均为游戏简化，不是完整真实运营模拟；未实现精确时刻表、全部待避追越与指定列车/座位的真实票制。
- 京王新線（初台、幡ヶ谷）、JR、井の頭線、更多地铁、成田机场任务、高尾山缆车等尚未开放；部分新站站外沿用通用场景。
- 存档保存 profile（余额、收藏、食品等），没有保存整趟旅程的当前位置/时刻/任务步骤；刷新会从家重新开始。保存依赖浏览器 localStorage，写入异常目前静默处理，无云端备份。
- 测试含本机绝对路径及旧线路场景假设；旧测试可能因版本变化不再适用，不能直接以旧用例失败判定游戏新回归。
- `game.js` 仍是大型单文件，保留旧相模原线引擎及重复数据/旧提示；当前玩家主流程使用新版直通引擎。本次按要求不清理、不重构。
- 广播使用系统日语语音，实际可用声音因设备而异。手机测试是390×844浏览器视口，非真实手机实机验证。

## 主要文件与架构

- `index.html`：Canvas、HUD、菜单、触屏及快进控件；顺序加载 data.js、game.js。
- `style.css`：桌面/移动端布局与样式。
- `data.js`：相模原线站序、种别、地方任务和收藏数据。
- `game.js`：IIFE内场景状态机、步行寻路、Canvas绘图、家庭/购物/餐厅/公园、列车时刻/状态、任务、音频、localStorage与只读 gameDebug。
- `MAIN_STATIONS` / `RAIL_ROUTES` / `makeMainTrain` / `throughState`：当前京王直通网络；单轨、动物园线另有状态机。旧相模原线逻辑仍保留。
- `tests/`：历代 Playwright 回归与视觉用例。`tests/helpers.cjs` 绑定本机运行时、Chrome及项目路径。
- `artifacts/`：历史截图、源码备份和验证记录。`怎么玩.txt`：当前玩法与实物资料。

## 不要随意重构的部分

保留可双击离线启动、原始扭蛋往返任务、手动下车和坐过站规则、车内空座、IC连续行程、真实分岔与种别停车、步行换乘、购物/烹饪保存以及手机控件。不要改变已有站点索引、localStorage键 `tokyo-little-rail-v1`、version=1 / mainSchema=2迁移规则，除非先写迁移与回归验证。不要删除历史测试、备份与截图，也不要把互动退化成瞬移或静态地图。

## 下一步建议（须用户再次授权）

先维持当前冻结状态。如恢复工作，优先确认当前版回归入口与测试路径可移植性，按具体复现修 bug；通过原始往返与三条直通行程后再考虑高尾山口站前/缆车等新内容。不要借交接进行架构重写或继续扩线。
