// Rendering fixtures only; gameplay is exercised separately through public controls.
const fs=require('fs');const{chromium}=require('/Users/ri/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');
(async()=>{const b=await chromium.launch({executablePath:'/Applications/Google Chrome.app/Contents/MacOS/Google Chrome',headless:true});for(const [name,fixture]of[['restaurant',"station=0;scene='restaurant';player={x:440,y:420};dining={seat:0,status:'served',x:850,y:475,route:[],until:999};"],['mono-car',"station=7;scene='monoCar';player={x:600,y:620};time=25;monoRide={start:0,dir:1};"]]){const p=await b.newPage({viewport:{width:390,height:844}}),errors=[];p.on('pageerror',e=>errors.push(e.message));await p.setContent(fs.readFileSync('index.html','utf8').replace(/<script[^>]*><\/script>/g,''));await p.addStyleTag({content:fs.readFileSync('style.css','utf8')});await p.addScriptTag({content:fs.readFileSync('data.js','utf8')});await p.addScriptTag({content:fs.readFileSync('game.js','utf8').replace('updateUI();requestAnimationFrame(frame);',fixture+"started=true;profile.sound=false;$('welcome').hidden=true;updateUI();requestAnimationFrame(frame);")});await p.waitForTimeout(150);await p.screenshot({path:`artifacts/v3-${name}-mobile-final.png`});assert.deepEqual(errors,[]);await p.close();}await b.close();console.log('Mobile dining and monorail render fixtures PASS');})().catch(e=>{console.error(e);process.exit(1)});
